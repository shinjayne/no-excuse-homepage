import { SIX_SHOP_LINKS } from '@/app/menu'

export const metadata = {}

export default function Page() {
  return (
    <div
      className={'flex items-center justify-center w-screen h-screen bg-black'}
    >
      <h1 className={'text-red-600'}>redirecting...</h1>
    </div>
  )
}
